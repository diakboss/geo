import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { DeplacementPage } from './deplacement';

@NgModule({
  declarations: [
    DeplacementPage,
  ],
  imports: [
    IonicPageModule.forChild(DeplacementPage),
  ],
})
export class DeplacementPageModule {}
